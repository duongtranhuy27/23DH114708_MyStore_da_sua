@model _23DH114708_MyStore.Models.Product

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="bg-success">XÓA SẢN PHẨM </h2>

<h4 class="alert-danger">Bạn thật sự muốn xóa sản phẩm này à?</h4>

@Html.Partial("PVProductInfo", Model)

<div class="form-group">
    <div class="col-md-offset-4 col-md-8">
        @Html.ActionLink("Xóa", "Delete", new { id = Model.ProductID },
           new { @class = "btn btn-primary" })
        @Html.ActionLink("Quay lại danh sách", "Index", null,
        new { @class = "btn btn-primary" })
    </div>

</div>
